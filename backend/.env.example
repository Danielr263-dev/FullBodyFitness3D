# Local dev uses SQLite so there's nothing to install to get started.
# Swap this to a MySQL or Postgres connection string for production
# (see prisma/schema.prisma datasource comment for how).
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-something-random"
PORT=4000
